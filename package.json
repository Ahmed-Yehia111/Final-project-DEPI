{
  "name": "pneumoscope",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first chest X-ray pneumonia screening UI backed by a trained ResNet-50 Keras model.",
  "scripts": {
    "setup": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/setup.ps1",
    "dev": "pnpm --parallel --filter @pneumoscope/api --filter @pneumoscope/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm typecheck && pnpm test && powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/smoke-model.ps1"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@11.7.0",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

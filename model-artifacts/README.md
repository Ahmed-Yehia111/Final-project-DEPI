# Model Artifacts

Place the private trained model here for the default local run path:

```text
model-artifacts/best_model_auc.keras
```

Model files are intentionally ignored by Git. The current `.keras` artifact is about 389 MB, which is too large for normal GitHub repository history and should remain private with the project owner.
